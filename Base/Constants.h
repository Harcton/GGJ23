#pragma once

namespace constants
{
	constexpr float worldSize = 500.0f;
	constexpr float coreSize = 25.0f;
	constexpr float defaultRootLength = 20.0f;
	constexpr se::time::Time gameWinTime = se::time::fromSeconds(60.0 * 5.0);
	constexpr se::Color rootColor{ 0x25150BFF };
}
